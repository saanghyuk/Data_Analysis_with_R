v <- 4+12
# v <- 4+12 할당한다. 
#실행시키기 -> 1. 커서 위치에서 RUN 누르기. 2. 커서 위치에서 control  + enter누르기 3. 드레그 하고 + RUN
v
#전체를 다 드레그 하고, 위 1 혹은 2번을 누르면 전체가 실행된다. 
#대문자/소문자를 구분해야 한다.   v <- 10 != V<- 10 
w <- v*2
x <- v/2

y <- sqrt(v) # root 를 의미. 
z <- x**2 #제곱은 별표를 두개 붙인다. 
a <- v+w+x+y+z
a

#콘솔은 언제나 화살표 모양으로 시작되어 있다.

#소스창과 콘솔창의 차이점. 
# 소스창: 입력
# 콘솔창: 출력 (항상 > 표시로 시작되어 있음.)

#object : 객체를 의미함. 어떤 값의 몸체를 이ㅡ미함. 


#1, 2, 3읽어오기 -> 수업은 4장부터 진행한다. 